type: "library"
language: "typescript"

tasks:
  build:
    options:
      mergeArgs: "replace"
    args: "--entry src/index.ts --entry src/axiom.logger.ts --clean --dts --format esm"
    outputs:
      - "dist"
