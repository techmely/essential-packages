$schema: https://moonrepo.dev/schemas/workspace.json
projects:
  - ts-packages/*
vcs:
  defaultBranch: main
  manager: git
hasher:
  optimization: "performance"
runner:
  cacheLifetime: "24 hours"
telemetry: false
hooks:
  commit-msg:
    - echo "hello commit msg"
  pre-commit:
    - "bun run test.unit.run"
  pre-push:
    - echo "hello push"
  prepare-commit-msg:
    - echo "hello prepare"
