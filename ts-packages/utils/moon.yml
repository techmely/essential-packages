type: "library"
language: "typescript"

workspace:
  inheritedTasks:
    exclude: ["publish"]

tasks:
  build:
    options:
      mergeArgs: "replace"
    args: "--dts --config tsup.config.ts"
    outputs:
      - "dist"
  publish:
    command: "node ../../scripts/publishPkg.mjs"
